// used to create new array populated with results

// syntax
// map((element,index,array) => )
// element -> temp
// index -> current index

const arr1 = [1,2,3,4]
const arr2 = arr1.map((x,y) => x*y)
console.log(arr2);